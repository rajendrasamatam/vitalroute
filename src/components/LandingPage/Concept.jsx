import React from 'react';
import TrafficScroll from './TrafficScroll';

const Concept = () => {
    return (
        <section className="vital-section" style={{ padding: 0, overflow: 'hidden' }}>
            <TrafficScroll />
        </section>
    );
};

export default Concept;
